<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/header.ejs") %>
</head>
<body>
    <%- include("../partials/nav.ejs") %>
<div class="container">
    <h2><%= exercise.name.toUpperCase() %></h2>
        <p>Equipment Used: <%= exercise.equipment %></p>
        <p>Reps: <%= exercise.reps %></p>
        <p>Sets: <%= exercise.sets %></p>
        <p>Weight Lifted: <%= exercise.weight %></p>
        <% if(exercise.completed === true) { %>
            <p>Completed Exercise? Yes</p>
        <% } else { %>
            <p>Completed Exercise? No</p>
        <% } %>
    <form action="/workouts/<%= exercise._id %>?_method=DELETE" method="POST">
        <button type="submit" class="btn btn-primary">Delete Exercise</button>
    </form>
    <form action="/workouts/<%= exercise._id %>/edit" method="GET">
        <button type="submit" class="btn btn-primary">Edit Exercise</button>
    </form>
   
</div>
</body>
</html>